import 'package:flutter/material.dart';


class CashReceipts extends StatelessWidget {
  const CashReceipts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
