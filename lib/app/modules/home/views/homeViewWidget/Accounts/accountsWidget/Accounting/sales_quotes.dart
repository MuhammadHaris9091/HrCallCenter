import 'package:flutter/material.dart';

class SalesQuotes extends StatelessWidget {
  const SalesQuotes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.cyan,
    );
  }
}
